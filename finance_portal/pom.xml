<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.3.9</version>
		<relativePath/>
	</parent>
	<groupId>com.financeportal</groupId>
	<artifactId>finance_portal</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>Finance Portal</name>
	<description>Finance Portal Backend</description>
	<properties>
		<java.version>21</java.version>
		<jacoco.version>0.8.11</jacoco.version>

		<!-- JaCoCo .exec dosya yolları — ${user.home}/.jacoco/ (ASCII path).
		     project.build.directory (Türkçe karakterli proje yolu) kullanırsak Windows'ta
		     Türkçe 'İ' karakteri surefire fork JVM'inde mangle olup .exec dosyası
		     yanlış klasöre yazılıyor ve report execution dosyayı bulamıyor. -->
		<jacoco.unit.exec>${user.home}/.jacoco/finance-portal-unit.exec</jacoco.unit.exec>
		<jacoco.it.exec>${user.home}/.jacoco/finance-portal-it.exec</jacoco.it.exec>

		<!-- SonarQube — sonar:sonar hedefi için varsayılan ayarlar.
		     Token CLI'dan veya .mvn/maven.config'den gelir (docs/sonar.md). -->
		<sonar.projectKey>finance-portal-backend</sonar.projectKey>
		<sonar.projectName>Finance Portal Backend</sonar.projectName>
		<!-- Lokal SonarQube docker container (docs/sonar.md icin).
		     CLI'dan override edilebilir: -Dsonar.host.url=https://my-sonar.example -->
		<sonar.host.url>http://localhost:9000</sonar.host.url>
		<sonar.java.source>21</sonar.java.source>
		<sonar.sourceEncoding>UTF-8</sonar.sourceEncoding>
		<sonar.coverage.jacoco.xmlReportPaths>${project.basedir}/target/site/jacoco/jacoco.xml</sonar.coverage.jacoco.xmlReportPaths>
		<sonar.junit.reportPaths>${project.basedir}/target/surefire-reports,${project.basedir}/target/failsafe-reports</sonar.junit.reportPaths>
		<!-- Coverage'a dahil EDİLMEYECEK paketler: DTO/entity/enum POJO,
		     konfigürasyon class'ları, main app entry. -->
		<sonar.coverage.exclusions>
			**/model/dto/**,
			**/model/entity/**,
			**/model/enums/**,
			**/dto/**,
			**/config/**,
			**/*Application.java
		</sonar.coverage.exclusions>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter</artifactId>
			<exclusions>
				<exclusion>
					<groupId>org.springframework.boot</groupId>
					<artifactId>spring-boot-starter-logging</artifactId>
				</exclusion>
			</exclusions>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-log4j2</artifactId>
		</dependency>

		<dependency>
			<groupId>org.apache.logging.log4j</groupId>
			<artifactId>log4j-layout-template-json</artifactId>
			<version>2.20.0</version>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-redis</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
		</dependency>
		<!-- Keycloak Admin Client -->
		<dependency>
			<groupId>org.keycloak</groupId>
			<artifactId>keycloak-admin-client</artifactId>
			<version>24.0.1</version>
		</dependency>
		<!-- Mail (price alarm tetiklendiğinde e-posta bildirimi için) -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-mail</artifactId>
		</dependency>
		<!-- Thymeleaf — e-posta template'i için -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-thymeleaf</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>2.6.0</version>
		</dependency>

		<dependency>
			<groupId>org.springframework.kafka</groupId>
			<artifactId>spring-kafka</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-ldap</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-elasticsearch</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
		</dependency>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>com.yahoofinance-api</groupId>
			<artifactId>YahooFinanceAPI</artifactId>
			<version>3.17.0</version>
		</dependency>
		<dependency>
			<groupId>org.jsoup</groupId>
			<artifactId>jsoup</artifactId>
			<version>1.17.2</version>
		</dependency>
		<dependency>
			<groupId>com.fasterxml.jackson.datatype</groupId>
			<artifactId>jackson-datatype-jsr310</artifactId>
		</dependency>

		<!-- Metrics: Micrometer Prometheus registry -> /actuator/prometheus.
		     Prometheus bu ucu doğrudan scrape eder (Collector çökse bile backend
		     liveness/JVM/HTTP metrikleri görünür). -->
		<dependency>
			<groupId>io.micrometer</groupId>
			<artifactId>micrometer-registry-prometheus</artifactId>
		</dependency>

		<!-- Traces: OpenTelemetry Java Agent (-javaagent) ile bytecode
		     instrument ediliyor (bkz. run-dev.ps1 / scripts/download-otel-agent.ps1).
		     Bu yüzden in-code micrometer-tracing-bridge-otel + otlp exporter
		     KALDIRILDI — ikisi birden GlobalOpenTelemetry'yi set etmeye çalışıp
		     çakışır. Agent tek trace kaynağıdır. -->
		<!-- OTel Java Agent SDK'yı kendi taşır; ek runtime bağımlılığı gerekmez. -->
		<dependency>
			<groupId>io.opentelemetry.instrumentation</groupId>
			<artifactId>opentelemetry-instrumentation-annotations</artifactId>
			<version>2.4.0</version>
		</dependency>

		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.mapstruct</groupId>
			<artifactId>mapstruct</artifactId>
			<version>1.5.5.Final</version>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- H2 in-memory DB — `test` profile için (application-test.yml).
		     Mevcut Mockito-based unit test'ler kullanmaz; full @SpringBootTest
		     integration test'leri için gerekir. -->
		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter</artifactId>
			<version>5.9.3</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.mockito</groupId>
			<artifactId>mockito-core</artifactId>
			<version>5.2.0</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.mockito</groupId>
			<artifactId>mockito-junit-jupiter</artifactId>
			<version>5.5.0</version>
			<scope>test</scope>
		</dependency>
		<!-- Apache Commons Codec (Base32 encoding için) -->
		<dependency>
			<groupId>commons-codec</groupId>
			<artifactId>commons-codec</artifactId>
			<version>1.15</version>
		</dependency>

	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
					<!-- mvnw spring-boot:run forked JVM'ine UTF-8 zorla.
					     Windows default cp1254 → "EMTİA" → "EMT─░A" mojibake'ini önler. -->
					<jvmArguments>-Dfile.encoding=UTF-8 -Dstdout.encoding=UTF-8 -Dstderr.encoding=UTF-8</jvmArguments>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.flywaydb</groupId>
				<artifactId>flyway-maven-plugin</artifactId>
				<version>10.10.0</version>
				<configuration>
					<url>${env.DB_URL}</url>
					<user>${env.DB_USERNAME}</user>
					<password>${env.DB_PASSWORD}</password>
					<locations>
						<location>filesystem:src/main/resources/db/migration</location>
					</locations>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.11.0</version>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.mapstruct</groupId>
							<artifactId>mapstruct-processor</artifactId>
							<version>1.5.5.Final</version>
						</path>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
							<version>${lombok.version}</version>
						</path>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok-mapstruct-binding</artifactId>
							<version>0.2.0</version>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>

			<!-- Surefire = unit testler. *Test.java / *Tests.java; *IT.java HARİÇ.
			     mvn test ile çalışır.
			     argLine: JaCoCo prepare-agent'ın set ettiği surefireArgLine property'sini
			     fork edilen test JVM'ine geçir (yoksa coverage .exec dosyası üretilmez). -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<argLine>${surefireArgLine}</argLine>
					<includes>
						<include>**/*Test.java</include>
						<include>**/*Tests.java</include>
					</includes>
					<excludes>
						<exclude>**/*IT.java</exclude>
					</excludes>
				</configuration>
			</plugin>

			<!-- Failsafe = integration testler. *IT.java pattern'i.
			     mvn verify ile çalışır (integration-test + verify goal'leri).
			     Testcontainers / WireMock gibi ağır bağımlılıklarla çalışan testler buraya.
			     argLine: JaCoCo IT agent'ı (failsafeArgLine) forke geçir. -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-failsafe-plugin</artifactId>
				<configuration>
					<argLine>${failsafeArgLine}</argLine>
					<includes>
						<include>**/*IT.java</include>
					</includes>
				</configuration>
				<executions>
					<execution>
						<goals>
							<goal>integration-test</goal>
							<goal>verify</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<!-- JaCoCo = test coverage. Üç execution:
			     1) prepare-agent  → surefireArgLine property'sine -javaagent satırını yazar
			        (Surefire bu satırı argLine olarak unit test JVM'ine geçirir)
			     2) prepare-agent-integration → failsafeArgLine property'sine IT agent satırı
			     3) report (verify) → target/site/jacoco/ altında HTML + XML rapor üretir.
			        XML'i SonarQube okur (sonar.coverage.jacoco.xmlReportPaths).
			     Not: Property isimlerini ayrı tutmazsak ikinci execution birincisini ezer
			     ve unit coverage kaybolur. -->
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>${jacoco.version}</version>
				<executions>
					<execution>
						<id>prepare-agent</id>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
						<configuration>
							<propertyName>surefireArgLine</propertyName>
							<!-- destFile: project.build.directory yerine ASCII path.
							     Sebep: proje klasöründe Türkçe 'İ' var, surefire fork JVM'i
							     argLine'daki path'i cp1254 → Latin-1 mangle ediyor
							     (örn. "BİT" → "BÝT") ve .exec yanlış yere yazılıyor. -->
							<destFile>${jacoco.unit.exec}</destFile>
						</configuration>
					</execution>
					<execution>
						<id>prepare-agent-integration</id>
						<goals>
							<goal>prepare-agent-integration</goal>
						</goals>
						<configuration>
							<propertyName>failsafeArgLine</propertyName>
							<destFile>${jacoco.it.exec}</destFile>
						</configuration>
					</execution>
					<execution>
						<id>report</id>
						<phase>verify</phase>
						<goals>
							<goal>report</goal>
						</goals>
						<configuration>
							<dataFile>${jacoco.unit.exec}</dataFile>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<!--
		    Tek komutla Sonar push: ./mvnw -Psonar
		    Zincir: clean -> test (JaCoCo agent .exec yazar) -> verify (jacoco:report -> XML uretir)
		            -> sonar:sonar (XML'i okuyup dashboard'a coverage ile push eder).
		    Integration testler skip (-Psonar profilinde skipITs=true).
		    NOT: sonar.token .mvn/maven.config (gitignored). Token yoksa sonar:sonar
		         "not authorized" verir -> UI'den token uret.
		-->
		<profile>
			<id>sonar</id>
			<properties>
				<skipITs>true</skipITs>
			</properties>
			<build>
				<defaultGoal>clean verify sonar:sonar</defaultGoal>
			</build>
		</profile>
	</profiles>
</project>
