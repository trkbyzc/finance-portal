input {
  kafka {
    bootstrap_servers => "finance-kafka:29092" # Tireli isim
    topics => ["finance-app-logs"]
    codec => "json"
    group_id => "finance-logstash-group"
  }
}

output {
  opensearch {
    hosts => ["http://finance-opensearch:9200"] # Tireli isim
    index => "finance-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}